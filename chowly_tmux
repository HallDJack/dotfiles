# Turn remain on exit on
set-remain-on-exit on

# Chowly session
new -s Chowly -n chowly -c ~/Code/chowly-inc/chowly
splitw -h -p 30 -t 1 -c ~/Code/chowly-inc/chowly
splitw -v -p 50 -t 1 -c ~/Code/chowly-inc/chowly

# dotfiles
neww -n dotfiles -t 9 -c ~/Code/dotfiles
selectw -t 9
splitw -h -p 50 -t 9 -c ~/Code/dotfiles

# select window 0
selectw -t 0
